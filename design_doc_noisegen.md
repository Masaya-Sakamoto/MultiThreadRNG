# （参考）計算・データ管理部分

## 構成

- Queue
- 単位データ生成関数（外部・子クラス）
- イベント開始ハンドラ関数
- memcpy request関数　（外部）
- 管理側



# データ生成部分

## 要件

- c++でプログラムを記述する
- 一つ一つの乱数配列は、後に指定する乱数配列生成関数を利用して作る
- publicのメンバ関数はコンストラクタ、デストラクタ、getter関数、リソース開放関数の４つ
- getterが呼ばれると乱数配列がすでにコピーされた、コピー先の配列のポインタを返す
- リソース開放関数を呼ぶと、新たな乱数配列が生成されて元

## クラス生成時の引数

- 配列サイズ:size_t
- 使用可能スレッド数:size_t
- 配列をコピー可能なポインタのリスト
- 配列転送用関数のポインタ
    - cudaMemcpyAsyncに類似
    - コンパイル時に決定される
    - GPUまたはCPUをその後の計算で利用するため、この関数を変化させる必要がある。
- このプログラムが利用するバッファの数:size_t
- 生成予定タスクQueue
- （生成中）スレッドIDのQueue
- 生成完了後の配列ポインタQueue
- 転送中Queue
- （乱数に限る）shuffle用一時保管Queue -- データ送信後にこちらにpushする。
- （乱数に限る）shuffle用領域

## private function

getActive() = N(生成予定タスクQueue) + N(生成中Queue) + N(生成完了Queue) + N(転送中Queue) - {tmpQueueMax - N(tmpQueue) <!-- 転送中Queueにtmpのポインタも入っているのでそれを除いた値が有効なデータ数 -->}

## public function

getInactive(): 最大データ数 - getActive()

## クラス生成時

最大データ数だけデータ生成予定Queueに追加
